const nombre = "deadpool";
const real = " wade winston";
const completo = nombre + " " + real;
const templ = `el nombre real de ${nombre} es${real}`;

console.log(completo);
console.log(templ);
console.log(completo == templ);

const html = `
<h1>Hola mundo</h1>
<p>Aloha</p>`;

console.log(html);
